<UserControl x:Class="KKTD_V3.Views.LiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:KKTD_V3.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:LiveViewModel}"
             d:DesignHeight="700" d:DesignWidth="1400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="360" />
        </Grid.ColumnDefinitions>

        <!-- Live-Bild + Bounding-Box-Overlay -->
        <Border Background="#0B1220" Margin="8" CornerRadius="4" ClipToBounds="True">
            <Grid>
                <!-- Headline liegt hinter dem Image; sobald LiveBitmap kommt deckt es ihn ab. -->
                <TextBlock Text="{Binding HeadlineText}"
                           Foreground="#9CA3AF"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="16" />
                <Viewbox Stretch="Uniform">
                    <Grid Width="{Binding ImageWidth}" Height="{Binding ImageHeight}">
                        <Image Source="{Binding LiveBitmap}"
                               Stretch="Fill"
                               RenderOptions.BitmapScalingMode="LowQuality" />
                        <ItemsControl ItemsSource="{Binding Detections}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas IsItemsHost="True" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Canvas.Left" Value="{Binding BoundingBox.X}" />
                                    <Setter Property="Canvas.Top"  Value="{Binding BoundingBox.Y}" />
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Rectangle Stroke="Lime" StrokeThickness="8"
                                               Width="{Binding BoundingBox.Width}"
                                               Height="{Binding BoundingBox.Height}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Viewbox>
            </Grid>
        </Border>

        <!-- Right pane -->
        <StackPanel Grid.Column="1" Margin="0,8,8,8">
            <Border Background="#F3F4F6" Padding="12" CornerRadius="4" Margin="0,0,0,8">
                <StackPanel>
                    <TextBlock Text="Status" FontWeight="Bold" />
                    <TextBlock Text="{Binding HeadlineText}" Margin="0,4,0,0" />
                    <TextBlock Margin="0,8,0,0">
                        <Run Text="Frames: " /><Run Text="{Binding FrameCount}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="FPS: " /><Run Text="{Binding Fps, StringFormat=F1}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Zykluszeit: " /><Run Text="{Binding LastProcessingTimeMs, StringFormat=F1}" /> ms
                    </TextBlock>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="Start" Command="{Binding StartCommand}" Padding="12,6" Margin="0,0,8,0" />
                <Button Content="Stop"  Command="{Binding StopCommand}"  Padding="12,6" />
            </StackPanel>

            <Border Background="#F3F4F6" Padding="12" CornerRadius="4" Margin="0,0,0,8">
                <StackPanel>
                    <TextBlock Text="Flat-Field" FontWeight="Bold" />
                    <TextBlock Text="{Binding FlatFieldStatus}" Margin="0,4,0,0" />
                    <CheckBox Content="Korrektur live anzeigen"
                              IsChecked="{Binding FlatFieldEnabled}"
                              Margin="0,8,0,0" />
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="Referenz aufnehmen"
                                Command="{Binding CaptureFlatFieldCommand}"
                                Padding="10,6" Margin="0,0,8,0" />
                        <Button Content="Verwerfen"
                                Command="{Binding ClearFlatFieldCommand}"
                                Padding="10,6" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Background="#F3F4F6" Padding="12" CornerRadius="4" Margin="0,0,0,8">
                <StackPanel>
                    <TextBlock Text="Detektion" FontWeight="Bold" />
                    <TextBlock Text="{Binding DetectionStatus}" Margin="0,4,0,0" />
                    <CheckBox Content="Bounding-Boxes anzeigen"
                              IsChecked="{Binding DetectionEnabled}"
                              Margin="0,8,0,0" />
                    <Button Content="Bild speichern"
                            Command="{Binding SaveSampleCommand}"
                            Padding="10,6" Margin="0,8,0,0"
                            HorizontalAlignment="Left" />
                </StackPanel>
            </Border>

            <TextBlock Text="Letzte Erkennungen" FontWeight="Bold" Margin="0,8,0,4" />
            <ListBox ItemsSource="{Binding RecentResults}" Height="280">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock>
                                <Run Text="Bahn " /><Run Text="{Binding Lane}" />
                                <Run Text=" Zone " /><Run Text="{Binding Zone}" />
                                <Run Text=" — " /><Run Text="{Binding Verdict}" />
                            </TextBlock>
                            <TextBlock Foreground="#6B7280">
                                <Run Text="Score: " /><Run Text="{Binding Score, StringFormat=F2}" />
                                <Run Text="  Fläche: " /><Run Text="{Binding AreaPx}" /><Run Text=" px" />
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>
